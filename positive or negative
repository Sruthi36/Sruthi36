#include<stdio.h>
int main()
{
	int number;
	printf("Enter a number:");
	scanf("%d",&number);
	if (number>=0)
		printf("%d is a Positive integer\n",number);
	else
		printf("%d is a Negative integer",number);
	return 0;
}
